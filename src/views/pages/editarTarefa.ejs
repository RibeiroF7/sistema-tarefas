<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar tarefa </title>
    <!-- <link rel="stylesheet" href="/public/css/styles.css"> -->
  </head>
  <style>
      *{
      box-sizing: border-box;
    }
    body{
      font-family: Georgia, 'Times New Roman', Times, serif;
    }
    h2{
      font-size: 1.80em;
      color: #3173f6;
      text-align: center;
      margin-bottom:2em;
    }
    .div-editar-tarefa{
      background: #f6f6f6;
      display: grid;
      width:360px;
      height:495px;
      border-radius:15px;
      padding:1em;
      justify-items: center;
      margin: auto;/*centraliza a div*/
      margin-top:5em;
    }
    form{
      display: grid;
      width:250px;
    }
    input,textarea,button,select{
      padding: 1em;
      margin:0.80em;
      border-radius:15px;
      border: none;
    }
    input::placeholder,
    textarea::placeholder{
      color: #9ebaf3;
    }
    button{  
      background: #3173f6;
      color: aliceblue;
    }
    button:hover{
      background: #4780f3;
    }
    a{
      text-decoration: none;
      color: #4780f3;
    }
    a:hover{
      color: #759ff5;
    }
  </style>
<body>
  <div class="div-editar-tarefa">
    <h2>Editar Tarefa</h2>

    <form action="/editar/<%= tarefa._id %>" method="POST">
      <input type="text" name="titulo" value="<%= tarefa.titulo %>" required />
      <textarea name="descricao"><%= tarefa.descricao %></textarea>
      <input type="date" name="prazo" value="<%= tarefa.prazo.toISOString().split('T')[0] %>" required />
    
      <select name="prioridade" required>
        <option value="baixa" <%= tarefa.prioridade === 'baixa' ? 'selected' : '' %>>Baixa</option>
        <option value="media" <%= tarefa.prioridade === 'media' ? 'selected' : '' %>>Média</option>
        <option value="alta" <%= tarefa.prioridade === 'alta' ? 'selected' : '' %>>Alta</option>
      </select>
    
      <button type="submit">Salvar ✔︎</button>
    </form>
    
    <a href="/dashboard">Cancelar ⨉</a>
  </div>    
</body>
</html>